let express,data,gpRouter,gRouter;_51f‍.w("express",[["default",["express"],function(v){express=v}]]);_51f‍.w("../data",[["default",["data"],function(v){data=v}]]);_51f‍.w("./routes/groceries-parameter",[["default",["gpRouter"],function(v){gpRouter=v}]]);_51f‍.w("./routes/groceries",[["default",["gRouter"],function(v){gRouter=v}]]);



const app = express();
const PORT = process.env.PORT ||3000;
app.use(express.json());




app.use('/api/v1', gpRouter);
app.use('/api/v1', gRouter);

// Assert that the groceries are sorted properly:
for (let i = 1; i < data.length; ++i) {
    if (data[i - 1].name >= data[i].name) {
        _51f‍.g.console.error("ERROR: Database ordering property violated", data[i - 1].name, data[i].name);
    }
}

// Assert that all existing groceries have valid categories
for (let i = 0; i < data.length; ++i) {
    // Check if it's a valid category
    let isValid = false;
    categories.forEach(cat => {
        if (data[i].category === cat) {
            isValid = true;
        }
    });
    if (!isValid) {
        console.error(`ERROR: Invalid category ${data[i].category}`);
    }
}

app.listen(PORT, () =>{
    console.log(`Server listening on port ${PORT}`);
});